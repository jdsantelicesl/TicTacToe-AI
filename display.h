#include <iostream>
#include <cmath>
#include <string>
using namespace std;

/*
Process: Updates the board state with either an X or O character. Prints out the board to display to player.
Input: 
    square: the square to be changed
    player: The player that is making the move (X or O)
    state: the board state.
Output: 
    state: arrays are passed by the address of first element. The state array will be changed to display the change.
*/
void updateBoard(int square, bool player, int state[]) {

}


/*
Process: Gets player input
Input: None
Output: the number of the square
*/
int getInput() {

}